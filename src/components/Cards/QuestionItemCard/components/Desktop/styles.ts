import styled from "styled-components";
import { Container, Subject } from '../../../common-styles';

export const DesktopContainer = styled(Container)`
    min-height: 80px;
`;

export const DesktopSubject = styled(Subject)`
    max-width: 200px;
`;